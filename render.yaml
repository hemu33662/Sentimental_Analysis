services:
  - type: web
    name: sentiment-analysis
    env: python
    region: singapore
    plan: free
    buildCommand: "./build.sh"
    startCommand: "cd BACKEND && gunicorn new_project.wsgi:application"
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
      - key: SECRET_KEY
        generateValue: true
      - key: WEB_CONCURRENCY
        value: 2
